@model IEnumerable<Models.Transact>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Compte.Libelle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeTransaction)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTransact)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Montant)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Compte.Libelle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeTransaction)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTransact)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Montant)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTransaction }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTransaction }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTransaction })
        </td>
    </tr>
}

</table>
